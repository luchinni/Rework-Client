const ResetPassword = () => {
    /* switch (name) {
    case "password":
      let passwordPattern:RegExp = /^(?=\w*\d)(?=\w*[A-Z])(?=\w*[a-z])\S{8,16}$/
      errors.password = passwordPattern.test(value)? "" : "Debe tener entre 8 y 16 caracteres, 1 mayúscula, 1 minúscula y un 1 número."
      break;
      case "passwordConfirmed":
        let passwordConfirmedPattern: RegExp =
        /^(?=\w*\d)(?=\w*[A-Z])(?=\w*[a-z])\S{8,16}$/; 
        errors.passwordConfirmed = passwordPattern.test(value) ?
        value === this.state.password ? "" 
        : "Las contraseñas no coinciden"
        : "Debe tener entre 8 y 16 caracteres y al menos 1 mayuscula, 1 minuscula y 1 número.";
        break;
    default:
        break;
    } */

    // hacer que se loguee nuevamente
    return(
        <div>

        </div>
    )
}

export default ResetPassword